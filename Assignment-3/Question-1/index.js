// Return the length of a given string.
function length(string) {
    return string.length
}
let a = "Nice Job"
let FinalLength = length(a)
console.log(`Length of the given string is ${FinalLength}`);
